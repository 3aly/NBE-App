import {StyleSheet} from 'react-native';

export const weak = StyleSheet.create({
  contianer: {
    flex: 0.5,

    flexDirection: 'column',
    justifyContent: 'center',
    alignItems: 'center',
  },

  image: {
    width: 150,
    height: 150,
  },
});
